POSTGRES_USER=synthetic
POSTGRES_PASSWORD=changeme
POSTGRES_DB=synthetic_data
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Service ports (change these to avoid conflicts with other projects)
DB_PORT=5432
API_PORT=8000
DASHBOARD_PORT=8501
MLFLOW_PORT=5005
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001

# MLflow tracking server
# Use http://mlflow:5000 for Docker services, http://localhost:${MLFLOW_PORT} for local scripts
MLFLOW_TRACKING_URI=http://localhost:5005
